{
    "version": 2,
    "builds": [
        {
            "src": "api/index.php",
            "use": "vercel-php@0.7.3",
            "config": {
                "build": "echo 'vercel.json: Starting Vercel build process...' && echo 'vercel.json: Building Vue.js frontend...' && (if [ -d 'frontend' ]; then cd frontend && echo 'vercel.json: Installing frontend dependencies...' && npm install && echo 'vercel.json: Running frontend build...' && npm run build && cd ..; else echo 'vercel.json: frontend directory not found, skipping Vue build.'; fi) && echo 'vercel.json: Frontend build complete.' && echo 'vercel.json: Caching Laravel backend...' && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan event:cache && echo 'vercel.json: Laravel caching complete. Build finished.'"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/index.php"
        },
        {
            "src": "/assets/(.*)",
            "headers": {
                "Cache-Control": "public, max-age=31536000, immutable"
            },
            "dest": "/frontend/dist/assets/$1"
        },
        {
            "src": "/(favicon\\.ico|manifest\\.webmanifest|robots\\.txt|img/.*)",
            "dest": "/frontend/dist/$1"
        },
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/frontend/dist/index.html"
        }
    ],
    "env": {
        "APP_NAME": "Laravel Vercel Vue",
        "APP_ENV": "production",
        "APP_DEBUG": "false",
        "APP_URL": "https://laravel-vercel-api.vercel.app",
        "APP_CONFIG_CACHE": "/tmp/config.php",
        "APP_EVENTS_CACHE": "/tmp/events.php",
        "APP_PACKAGES_CACHE": "/tmp/packages.php",
        "APP_ROUTES_CACHE": "/tmp/routes.php",
        "APP_SERVICES_CACHE": "/tmp/services.php",
        "CACHE_DRIVER": "array",
        "LOG_CHANNEL": "stderr",
        "SESSION_DRIVER": "array",
        "VIEW_COMPILED_PATH": "/tmp/views",
        "SSR_TEMP_PATH": "/tmp/ssr",
        "NODE_PATH": "node"
    }
}
